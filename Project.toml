name = "CesiumSlate"
uuid = "037ed0f0-9d4a-4e15-a26b-b7623adf1440"
version = "0.1.0"
authors = ["Kahli Burke <kahli@kahliburke.com>"]

[deps]
SlateExtensionsBase = "bc31d39e-4442-48fa-b9ae-1bd99fed63f7"

# The lean Slate extension SDK — the ENTIRE dependency. CesiumSlate adds an interactive CesiumJS globe to
# Slate as a rich-output component, depending only on this contract (never on the heavy KaimonSlate server).
# Dev-sourced from the sibling checkout while SEB is unregistered.
[sources]
SlateExtensionsBase = {path = "../KaimonSlate-widgets/lib/SlateExtensionsBase"}

[compat]
SlateExtensionsBase = "0.1"
julia = "1.10"
